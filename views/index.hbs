extends layout

block content
	h1= title
	p Welcome to HyperPooch
	br
	if user
		a(href='/hyperpooch') HyperPooch
		p Hello, #{user._name}
		a(href='/logout') logout
		
	else
		a(href='/login') login
		a(href='/register') register